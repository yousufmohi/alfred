# Alfred 🤖

Alfred is a code reviewing AI agent that reviews your local code and GitHub PR's.


### <a name="install-and-build">Install & Build 🛠️</a>

Clone the repository into your machine and run the install scripts (depending on your environment).
```console
git clone https://github.com/yousufmohi/alfred
cd alfred
./install.ps1
```

### <a name="run">Running Alfred 🔎</a>

When you run the install scripts the alfred alias should be set up on your system.

Commands

| Command                                 | What it does                         |
| --------------------------------------- | ------------------------------------ |
| `alfred setup`                            | Used to enter Anthropic API key     |
| `alfred revert`                           | Removes anthropic API key |
| `alfred review <file-name>`               | Reviews file |

